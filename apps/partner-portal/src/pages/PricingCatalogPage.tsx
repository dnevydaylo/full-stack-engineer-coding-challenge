import { Box, Typography } from '@mui/material';

export function PricingCatalogPage(): JSX.Element {
  return (
    <Box>
      <Typography variant="h1">Pricing Catalog</Typography>
      <Typography variant="body1" color="text.secondary">
        Must be implemented.
      </Typography>
    </Box>
  );
}
